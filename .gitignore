node_modules/
.next/
out/
build/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
/drizzle/meta

# Foundry
#
# `contracts/lib/` used to be ignored here, and that one line is why continuous
# integration was red. The workflow checks the repo out with `submodules:
# recursive` and runs `forge build`, but neither dependency was ever committed,
# so every run since the job was added has failed on a missing OpenZeppelin
# import. The README meanwhile claimed a passing test suite. Build artefacts
# are still ignored; the sources the build needs are not.
contracts/out/
contracts/out-recipientbound/
contracts/cache/
contracts/broadcast/
.env.local

# never commit any env variant, including backups
.env*
!.env.example

# granted session keys — key material
.sessions/

# agent strategy state
.agent-state/

# per-mandate benchmark marks

# built verifier package
packages/mandate-verify/dist/

# Narrow-viewport captures from `npm run shots:mobile`. Checked by eye, not committed.
.shots-390/

# The prompt this rebuild was worked from, kept out of the repo.
new_prompt.md
_shot.mjs
_overflow.mjs

# Generated by tsc --build
tsconfig.tsbuildinfo

# Working notes, kept on disk and out of the repository.
#
# Judges read whatever is in the tree. Planning documents, the prompt brief and
# a competitive strategy are not the product, and a self-assessment written
# before the marketplace rebuild contradicts the numbers the README now carries.
# None of it is deleted; it simply is not published.
docs/archive/
docs/video/
docs/REBUILD-PLAN.md
docs/DONE.md

# brand kit build
tools/brand/out/
tools/brand/.venv/
